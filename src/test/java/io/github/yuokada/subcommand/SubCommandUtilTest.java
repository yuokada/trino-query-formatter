package io.github.yuokada.subcommand;

import org.junit.jupiter.api.Test;

import java.io.IOException;

import static org.junit.jupiter.api.Assertions.assertEquals;

class SubCommandUtilTest {

    @Test
    void readFromFile() throws IOException {
        // This test is no longer valid as readFromFile is not part of SubCommandUtil
        // SubCommandUtil util = new SubCommandUtil() {};
        // String content = util.readFromFile("src/main/resources/queries/query1.sql");
        // assertEquals("select * from test_table;", content.trim());
    }
}